<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Confirm modal</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet" />
    <style>
      /* Modal nền */
      .hidden {
        display: none;
      }

      .modal {
        display: flex;
        justify-content: center;
        align-items: center;
      }

      /* Hiệu ứng phóng to/thu nhỏ cho modalItem */
      #confirmItem {
        opacity: 0;
        transform: scale(0.8);
        transition: all 0.3s ease-in-out;
      }

      #confirmItem.modal-active {
        opacity: 1;
        transform: scale(1);
      }

      /* Định nghĩa thêm cho modal nền */
      .fixed {
        position: fixed;
      }

      .inset-0 {
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
      }

      .bg-opacity-50 {
        background-color: rgba(0, 0, 0, 0.5);
      }
    </style>
  </head>
  <body>
    <div id="confirmModal" class="hidden fixed inset-0 bg-gray-400 bg-opacity-50 flex items-center justify-center z-50">
      <div id="confirmItem" class="group select-none w-[400px] flex flex-col p-4 relative items-center justify-center bg-white border-4 border-black shadow-lg rounded-2xl">
        <div class="">
          <div class="text-center p-3 flex-auto justify-center">
            <svg fill="currentColor" viewBox="0 0 20 20" class="group-hover:animate-bounce w-12 h-12 flex items-center text-gray-600 fill-[#c4c4c4] mx-auto mb-4" xmlns="http://www.w3.org/2000/svg">
              <path
                clip-rule="evenodd"
                d="M9 2a1 1 0 00-.894.553L7.382 4H4a1 1 0 000 2v10a2 2 0 002 2h8a2 2 0 002-2V6a1 1 0 100-2h-3.382l-.724-1.447A1 1 0 0011 2H9zM7 8a1 1 0 012 0v6a1 1 0 11-2 0V8zm5-1a1 1 0 00-1 1v6a1 1 0 102 0V8a1 1 0 00-1-1z"
                fill-rule="evenodd"
              ></path>
            </svg>
            <h2 class="text-xl font-bold py-4 text-black font-poppins">Are you sure?</h2>

            <p class="font-bold text-[16px] text-gray-500 px-2">Do you really want to continue to delete this article?</p>
          </div>
          <div class="p-2 mt-2 text-center space-x-1 md:block">
            <button
              id="cancelButton"
              class="mb-2 md:mb-0 bg-[#c4c4c4] px-5 py-2 text-sm shadow-sm font-medium tracking-wider border-2 border-gray-200 hover:border-gray-300 text-black rounded-full hover:shadow-lg hover:bg-[#afafaf] transition ease-in duration-300"
            >
              Cancel
            </button>
            <button
              id="confirmButton"
              class="bg-red-500 hover:bg-transparent px-5 ml-4 py-2 text-sm shadow-sm hover:shadow-lg font-medium tracking-wider border-2 border-red-500 hover:border-red-500 text-white hover:text-red-500 rounded-full transition ease-in duration-300"
            >
              Confirm
            </button>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
